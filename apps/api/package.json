{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest --detectOpenHandles",
    "dev": "nodemon --exec \"node -r esbuild-register ./src/index.ts\" -e .ts",
    "db:generate": "yarn prisma generate",
    "db:migrate": "yarn prisma migrate deploy",
    "db:seed": "yarn prisma db seed",
    "start": "yarn db:migrate && yarn prisma db seed && node -r esbuild-register ./src/index.ts",
    "build": "tsc && yarn db:generate",
    "lint": "tsc --noEmit && TIMING=1 eslint src --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.0",
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.12",
    "@types/jest": "^26.0.22",
    "@types/morgan": "^1.9.2",
    "@types/node": "^15.12.2",
    "@types/supertest": "^2.0.11",
    "esbuild-register": "^2.5.0",
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.15",
    "prisma": "^3.12.0",
    "scripts": "*",
    "supertest": "^6.1.3",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  },
  "jest": {
    "preset": "scripts/jest/node"
  },
  "dependencies": {
    "@prisma/client": "^3.12.0",
    "apollo-server-core": "^3.6.7",
    "apollo-server-express": "^3.6.7",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "graphql": "^16.4.0",
    "logger": "*",
    "morgan": "^1.10.0"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
