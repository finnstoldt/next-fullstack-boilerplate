{
  "name": "admin",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 3001 --clearScreen false",
    "build": "vite build",
    "deploy": "vercel deploy dist --team=turborepo --confirm",
    "lint": "cross-env TIMING=1 eslint src/**/*.tsx --fix && tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@apollo/client": "^3.6.2",
    "@emotion/react": "^11.9.0",
    "@emotion/styled": "^11.8.1",
    "@mui/material": "^5.6.4",
    "graphql": "^16.4.0",
    "graphql-codegen": "*",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "ui": "*"
  },
  "devDependencies": {
    "@types/react": "^18.0.6",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.3.6",
    "cross-env": "^7.0.3",
    "scripts": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3",
    "vite": "^2.6.14"
  }
}
